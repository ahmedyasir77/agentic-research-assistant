import { SHARED_CONTRACT_VERSION } from '@ara/shared';

// Replaced in M5 by the Express server; kept runnable so the scaffold is never
// a shell that cannot start.
console.log(`agentic-research-assistant api — contract v${String(SHARED_CONTRACT_VERSION)}`);
